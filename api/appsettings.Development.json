{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "//": "DEV-ONLY signing key so a fresh clone runs with no setup. Production MUST override via the Jwt__Key environment variable.",
    "Key": "dev-only-insecure-signing-key-do-not-use-in-production-0123456789",
    "Issuer": "taskmanager"
  }
}
